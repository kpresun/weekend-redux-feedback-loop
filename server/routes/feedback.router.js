const express = require('express');
const router = express.router();
const pool = require('../modules/pool');

// This will get all feedback from the feedback
router.get('/')


// This will post to database entire feedback
router.post('/')